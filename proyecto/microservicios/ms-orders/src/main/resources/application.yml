server:
  port: 8082

spring:
  application:
    name: ms-orders
  datasource:
    url: jdbc:postgresql://localhost:5432/orders_db
    username: postgres
    password: "1234"
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: smartlogix-clave-super-secreta-para-secretear-2026
  expiration: 86400000

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always